{
  "version": 2,
  "installCommand": "npm install && npm install ../shared",
  "buildCommand": "npm run build --prefix ../shared && npm run build",

  "routes": [
    { "src": "^/api/?$", "status": 404 },
    { "src": "/api/(.*)", "dest": "/api/[...slug].js" }
  ]
}
