{
  "version": 2,
  "builds": [
    {
      "src": "src/api/index.ts",
      "use": "@vercel/node",
      "config": { "includeFiles": "../shared/**" }
    }
  ],
  "routes": [{ "src": "/api/(.*)", "dest": "src/api/index.ts" }]
}
